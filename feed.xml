<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.9.5">Jekyll</generator><link href="https://fzakaria.com/feed.xml" rel="self" type="application/atom+xml" /><link href="https://fzakaria.com/" rel="alternate" type="text/html" /><updated>2024-03-04T00:22:35+00:00</updated><id>https://fzakaria.com/feed.xml</id><title type="html">Farid Zakaria’s Blog</title><subtitle>I'm a software engineer, father and wishful amateur surfer. If you've come seeking my political views; you've found the wrong &lt;a href=&quot;https://fareedzakaria.com/&quot;&gt;Fareed&lt;/a&gt;.</subtitle><entry><title type="html">Visualizing GitHub workflow run length time</title><link href="https://fzakaria.com/2024/03/03/visualizing-github-workflow-run-length-time.html" rel="alternate" type="text/html" title="Visualizing GitHub workflow run length time" /><published>2024-03-03T23:24:00+00:00</published><updated>2024-03-03T23:24:00+00:00</updated><id>https://fzakaria.com/2024/03/03/visualizing-github-workflow-run-length-time</id><content type="html" xml:base="https://fzakaria.com/2024/03/03/visualizing-github-workflow-run-length-time.html"><![CDATA[<p>GitHub Actions have now become an integral part of many open source projects, providing a free &amp; powerful CI system. I am surprised however there is no provided way to visualize the run length time (or other meaningful metrics) of your actions.</p>

<blockquote>
  <p>🕵️ I did find a few other third-party solutions that either extract the data or themselves can be added <a href="https://github.com/marketplace/actions/workflow-telemetry">as a step</a> to your workflow to get similar visualizations. I wanted something <em>simpler</em>.</p>
</blockquote>

<p>I previously wrote a post about <a href="/2024/02/27/hermetic-but-at-what-cost.html">the cost of runfiles</a> which had become evident when we noticed our GitHub Bazel build workflow had slowed down by 50x.</p>

<p>After landing my fix, I wanted to <em>visualize</em> the run length time of the action; and objectively see if my fix had worked. <em>Trust <strong>but</strong> verify</em>.</p>

<!--more-->

<p>I found the <a href="https://docs.github.com/en/rest/actions/workflow-runs?apiVersion=2022-11-28#list-workflow-runs-for-a-repository">GitHub API</a> does provide the run time length of each workflow. The <a href="https://github.com/cli/cli">GitHub CLI</a> makes it easy to extract out the data as JSON.</p>

<p>The following command generates the 100 last runs of the <code class="language-plaintext highlighter-rouge">buildAndTestBazel.yml</code> workflow for the <code class="language-plaintext highlighter-rouge">openxla/stablehlo</code> repository. It then filters the data to only include the <code class="language-plaintext highlighter-rouge">startedAt</code> and <code class="language-plaintext highlighter-rouge">updatedAt</code> fields and saves it to a file called <code class="language-plaintext highlighter-rouge">json_data.json</code>.</p>
<div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>gh run list <span class="nt">--workflow</span> buildAndTestBazel.yml <span class="se">\</span>
<span class="go">              --repo openxla/stablehlo \
              --json startedAt,updatedAt \
              --status completed \
              --branch main \
</span><span class="gp">              --limit 100 &gt;</span><span class="w"> </span>json_data.json
</code></pre></div></div>

<p>This JSON file then served as the foundation for the next phase: visualization. Enter <a href="https://plotnine.org/">plotnine</a>, a Python plotting library that brings the declarative and powerful grammar of graphics (the philosophy behind ggplot2 in R) to the Python ecosystem. Plotnine enables creating complex plots from data in a DataFrame with minimal fuss, making it an ideal choice for our purpose.</p>

<blockquote>
  <p>🕵️ For those unfamiliar, ggplot (and by extension, plotnine) is predicated on the idea of building plots layer by layer, adding components such as scales, axes, and geometries incrementally. This approach not only makes the plot construction intuitive but also highly customizable. I really like using them graphing because it means I can commit reusable, reliable and reproducible code for generating my visualizations.</p>
</blockquote>

<p>Using another script, I transformed the JSON data into a Pandas DataFrame and then into time series graphs using plotnine. The final visualization was used to prove that my fix had worked and that the run length time had indeed decreased.</p>

<blockquote>
  <p>You can view the script at <a href="https://gist.github.com/fzakaria/b3ca5a1d3700714497925d026bd8105b">this gist - b3ca5a1d3700714497925d026bd8105b</a></p>
</blockquote>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">pandas</span> <span class="k">as</span> <span class="n">pd</span>
<span class="kn">from</span> <span class="nn">plotnine</span> <span class="kn">import</span> <span class="n">ggplot</span><span class="p">,</span> <span class="n">aes</span><span class="p">,</span> <span class="n">geom_line</span><span class="p">,</span> <span class="n">theme</span><span class="p">,</span> <span class="n">labs</span><span class="p">,</span> <span class="n">scale_x_datetime</span><span class="p">,</span> <span class="n">element_text</span><span class="p">,</span> <span class="n">geom_point</span>
<span class="kn">import</span> <span class="nn">json</span>
<span class="kn">from</span> <span class="nn">datetime</span> <span class="kn">import</span> <span class="n">datetime</span>

<span class="c1">#  $ gh run list --workflow buildAndTestBazel.yml --repo openxla/stablehlo --json startedAt,updatedAt --status completed --branch main --limit 100 &gt; json_data.json
# Assuming `json_data` is your JSON data loaded into a variable
# If your JSON is in a file, you can load it with:
</span><span class="k">with</span> <span class="nb">open</span><span class="p">(</span><span class="s">'json_data.json'</span><span class="p">,</span> <span class="s">'r'</span><span class="p">)</span> <span class="k">as</span> <span class="n">f</span><span class="p">:</span>
    <span class="n">json_data</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="n">load</span><span class="p">(</span><span class="n">f</span><span class="p">)</span>

<span class="c1"># Convert JSON data to DataFrame
</span><span class="n">df</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="n">DataFrame</span><span class="p">(</span><span class="n">json_data</span><span class="p">)</span>

<span class="c1"># Convert the startedAt and updatedAt columns to datetime
</span><span class="n">df</span><span class="p">[</span><span class="s">'startedAt'</span><span class="p">]</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="n">to_datetime</span><span class="p">(</span><span class="n">df</span><span class="p">[</span><span class="s">'startedAt'</span><span class="p">])</span>
<span class="n">df</span><span class="p">[</span><span class="s">'updatedAt'</span><span class="p">]</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="n">to_datetime</span><span class="p">(</span><span class="n">df</span><span class="p">[</span><span class="s">'updatedAt'</span><span class="p">])</span>

<span class="c1"># Calculate the elapsed time in seconds
</span><span class="n">df</span><span class="p">[</span><span class="s">'elapsedTime'</span><span class="p">]</span> <span class="o">=</span> <span class="p">(</span><span class="n">df</span><span class="p">[</span><span class="s">'updatedAt'</span><span class="p">]</span> <span class="o">-</span> <span class="n">df</span><span class="p">[</span><span class="s">'startedAt'</span><span class="p">]).</span><span class="n">dt</span><span class="p">.</span><span class="n">total_seconds</span><span class="p">()</span>

<span class="c1"># If your datetimes are in UTC, ensure the comparison Timestamp is also in UTC
</span><span class="n">now</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="n">Timestamp</span><span class="p">.</span><span class="n">utcnow</span><span class="p">()</span>
<span class="n">one_month_ago</span> <span class="o">=</span> <span class="n">now</span> <span class="o">-</span> <span class="n">pd</span><span class="p">.</span><span class="n">DateOffset</span><span class="p">(</span><span class="n">months</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>

<span class="c1"># Filter data to the last month, ensuring both datetimes are timezone-aware or both are naive
</span><span class="n">df_last_month</span> <span class="o">=</span> <span class="n">df</span><span class="p">[</span><span class="n">df</span><span class="p">[</span><span class="s">'startedAt'</span><span class="p">].</span><span class="n">dt</span><span class="p">.</span><span class="n">tz_localize</span><span class="p">(</span><span class="bp">None</span><span class="p">)</span> <span class="o">&gt;=</span> <span class="n">one_month_ago</span><span class="p">.</span><span class="n">tz_localize</span><span class="p">(</span><span class="bp">None</span><span class="p">)]</span>

<span class="c1"># Plot using plotnine
</span><span class="n">plot</span> <span class="o">=</span> <span class="p">(</span><span class="n">ggplot</span><span class="p">(</span><span class="n">df_last_month</span><span class="p">,</span> <span class="n">aes</span><span class="p">(</span><span class="s">'startedAt'</span><span class="p">,</span> <span class="s">'elapsedTime'</span><span class="p">))</span> <span class="o">+</span>
        <span class="n">geom_line</span><span class="p">()</span> <span class="o">+</span>
        <span class="n">theme</span><span class="p">(</span><span class="n">axis_text_x</span><span class="o">=</span><span class="n">element_text</span><span class="p">(</span><span class="n">rotation</span><span class="o">=</span><span class="mi">90</span><span class="p">,</span> <span class="n">hjust</span><span class="o">=</span><span class="mi">1</span><span class="p">))</span> <span class="o">+</span>
        <span class="n">labs</span><span class="p">(</span><span class="n">x</span><span class="o">=</span><span class="s">'Date Workflow Started'</span><span class="p">,</span> <span class="n">y</span><span class="o">=</span><span class="s">'Elapsed Time (seconds)'</span><span class="p">,</span> <span class="n">title</span><span class="o">=</span><span class="s">'Elapsed Time Series'</span><span class="p">)</span> <span class="o">+</span>
        <span class="n">scale_x_datetime</span><span class="p">(</span><span class="n">date_breaks</span><span class="o">=</span><span class="s">'1 day'</span><span class="p">,</span> <span class="n">date_labels</span><span class="o">=</span><span class="s">'%Y-%m-%d'</span><span class="p">))</span> <span class="o">+</span> <span class="n">geom_point</span><span class="p">()</span>

<span class="c1"># Display or save the plot
# To display the plot in a Jupyter notebook or similar environment, just use:
</span><span class="n">plot</span><span class="p">.</span><span class="n">show</span><span class="p">()</span>

<span class="c1"># To save the plot to a file, use:
</span><span class="n">plot</span><span class="p">.</span><span class="n">save</span><span class="p">(</span><span class="s">"elapsed_time_series.png"</span><span class="p">,</span> <span class="n">width</span><span class="o">=</span><span class="mi">4</span><span class="p">,</span> <span class="n">height</span><span class="o">=</span><span class="mi">4</span><span class="p">,</span> <span class="n">dpi</span><span class="o">=</span><span class="mi">150</span><span class="p">)</span>
</code></pre></div></div>

<blockquote>
  <p>😲 ChatGPT &amp; Gemini were both pretty good options at generating the code necessary to generate the graphs. Declarative language for tasks proves to be a powerful use for LLMs.</p>
</blockquote>

<p><img src="/assets/images/workflow_time_series.png" alt="Workflow graph" /></p>

<p>The visualized graph may <em>not be the prettiest</em> but it gets the job done and is reproducible for the dataset.</p>

<p>If you have suggestions on how to improve the visualization or the process, please let me know. Hopefully GitHub will provide a built-in way to visualize this data in the future. 🤞</p>]]></content><author><name></name></author><summary type="html"><![CDATA[GitHub Actions have now become an integral part of many open source projects, providing a free &amp; powerful CI system. I am surprised however there is no provided way to visualize the run length time (or other meaningful metrics) of your actions. 🕵️ I did find a few other third-party solutions that either extract the data or themselves can be added as a step to your workflow to get similar visualizations. I wanted something simpler. I previously wrote a post about the cost of runfiles which had become evident when we noticed our GitHub Bazel build workflow had slowed down by 50x. After landing my fix, I wanted to visualize the run length time of the action; and objectively see if my fix had worked. Trust but verify.]]></summary></entry><entry><title type="html">Hermetic, but at what cost?</title><link href="https://fzakaria.com/2024/02/27/hermetic-but-at-what-cost.html" rel="alternate" type="text/html" title="Hermetic, but at what cost?" /><published>2024-02-27T22:02:00+00:00</published><updated>2024-02-27T22:02:00+00:00</updated><id>https://fzakaria.com/2024/02/27/hermetic-but-at-what-cost</id><content type="html" xml:base="https://fzakaria.com/2024/02/27/hermetic-but-at-what-cost.html"><![CDATA[<blockquote>
  <p><strong>tl;dr;</strong> This is a little story about how making Bazel <em>hermetic</em> can lead to some unexpected consequences. In this particular case, it caused our GitHub action to slow down by 50x from 1 minute to over 60 minutes.</p>

  <p>The fix recommended was to apply the following to your <code class="language-plaintext highlighter-rouge">.bazelrc</code> – I <strong>needed</strong> to understand why however.</p>
  <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># Disabling runfiles links drastically increases performance in slow disk IO situations
# Do not build runfile trees by default. If an execution strategy relies on runfile
# symlink tree, the tree is created on-demand. See: https://github.com/bazelbuild/bazel/&gt; &gt; issues/6627
# and https://github.com/bazelbuild/bazel/commit/03246077f948f2790a83520e7dccc2625650e6df
build --nobuild_runfile_links
test --nobuild_runfile_links

# https://bazel.build/reference/command-line-reference#flag--legacy_external_runfiles
build --nolegacy_external_runfiles
test --nolegacy_external_runfiles
</code></pre></div>  </div>
</blockquote>

<!--more-->

<p><a href="https://bazel.build/">Bazel</a> is a popular build system for those seeking to adopt Google-style build methodologies &amp; kool-aid with the hopes of achieving hermetic nirvana.
Very quickly after adopting Bazel, you realize that although you’ve defined your build targets, ultimately the default toolchains provided by Bazel use the system provided binaries, libraries and headers.</p>

<blockquote>
  <p>📝 <a href="https://nixos.org/">NixOS</a> is a great way to bring in a reproducible environment for Bazel to use.</p>
</blockquote>

<p>Recently, I decided to “fix” our non-hermetic Python Bazel builds by adding a new reliance on <a href="https://github.com/bazelbuild/rules_python">@python_rules</a> which helps pull in a Python toolchain for Bazel to use.</p>

<blockquote>
  <p>📝 Toolchains in Bazel are often downloaded as pre-compiled binary blobs, so their reproducibility is still limited to and can vary according to the underlying system being run.</p>
</blockquote>

<p>Python is a toolchain but also a runtime file <a href="https://bazel.build/extending/rules#runfiles">“runfile”</a>, as the language is interpreted. The result of including <em>@python_rules</em> was that every Python test target (<em>py_test</em>) included a runfile tree for a complete Python installation.</p>

<p>Here is a sample of the symlinks for the Python installation that are created.</p>
<div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="go">❯ ls -l bazel-out/k8-fastbuild/mytest.test.runfiles/rules_python\~0.30.0\~python\~python_3_10_x86_64-unknown-linux-gnu/bin
</span><span class="gp">2to3 -&gt;</span><span class="w"> </span>/home/fmzakari/.cache/bazel/_bazel_fmzakari/17bce12c4b47a4a2fc75249afee05177/external/rules_python~0.30.0~python~python_3_10_x86_64-unknown-linux-gnu/bin/2to3
<span class="gp">2to3-3.10 -&gt;</span><span class="w"> </span>/home/fmzakari/.cache/bazel/_bazel_fmzakari/17bce12c4b47a4a2fc75249afee05177/external/rules_python~0.30.0~python~python_3_10_x86_64-unknown-linux-gnu/bin/2to3-3.10
<span class="gp">idle3 -&gt;</span><span class="w"> </span>/home/fmzakari/.cache/bazel/_bazel_fmzakari/17bce12c4b47a4a2fc75249afee05177/external/rules_python~0.30.0~python~python_3_10_x86_64-unknown-linux-gnu/bin/idle3
</code></pre></div></div>

<p>How many files (inodes) are created for each test?</p>

<div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="go">❯ find bazel-out/k8-fastbuild/bin/stablehlo/tests/transform_chlo.mlir.test.runfiles/rules_python\~0.30.0\~python\~python_3_10_x86_64-unknown-linux-gnu  | wc  -l
2458
</span></code></pre></div></div>

<p>🤯 <strong>2458</strong> 🤯</p>

<p>Let that sink in. Bazel will create <strong>2458</strong> inodes (symlinks) for each test target. It will do this by default for builds even if the tests are never run or executed.</p>

<p>To double down on this pain, Bazel supports two runfile trees for external repositories which is outlined in their <a href="https://github.com/bazelbuild/bazel/wiki/Updating-the-runfiles-tree-structure">wiki</a>.</p>

<p>🤯 So the whole tree exists at least twice for each test target. 🤯</p>

<p>This increase in symlinks caused our GitHub cache action to suddenly jump from 1 minute to over 60 minutes. This was a 50x increase in time to run the action. The <em>tar</em> and <em>untar</em> of the <code class="language-plaintext highlighter-rouge">.cache</code> directory had to process so many additional files that it was an IO bottleneck.</p>

<p>The recommended approach (from Bazel Slack and other web links) is to have the following in your <code class="language-plaintext highlighter-rouge">.bazelrc</code></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># Disabling runfiles links drastically increases performance in slow disk IO situations
# Do not build runfile trees by default. If an execution strategy relies on runfile
# symlink tree, the tree is created on-demand. See: https://github.com/bazelbuild/bazel/issues/6627
# and https://github.com/bazelbuild/bazel/commit/03246077f948f2790a83520e7dccc2625650e6df
build --nobuild_runfile_links
test --nobuild_runfile_links

# https://bazel.build/reference/command-line-reference#flag--legacy_external_runfiles
build --nolegacy_external_runfiles
test --nolegacy_external_runfiles
</code></pre></div></div>

<dl>
  <dt><strong>–nobuild_runfile_links</strong></dt>
  <dd>This causes runfiles to not be created during builds. Tests still work, because they have a feature to build the runfiles on demand if needed. The downside to this option, is you can’t run the test unless you do <code class="language-plaintext highlighter-rouge">bazel test</code> or <code class="language-plaintext highlighter-rouge">bazel run</code> which sets up the runfile tree.</dd>
  <dt><strong>–nolegacy_external_runfiles</strong></dt>
  <dd>Stops creating the symlinks in 2x places by no longer supported the legacy location for runfiles.</dd>
</dl>

<p>Bazel can provide some amazing guarantees but navigating the myriad of knobs can be frustrating.</p>

<p>Having had experience with NixOS – I’m curious why Bazel doesn’t support template support for the shebang to point to a single location for the Python interpreter.</p>

<p>Anyways, if you want to make your GitHub actions faster, consider adding the above to your <code class="language-plaintext highlighter-rouge">.bazelrc</code> file.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[tl;dr; This is a little story about how making Bazel hermetic can lead to some unexpected consequences. In this particular case, it caused our GitHub action to slow down by 50x from 1 minute to over 60 minutes. The fix recommended was to apply the following to your .bazelrc – I needed to understand why however. # Disabling runfiles links drastically increases performance in slow disk IO situations # Do not build runfile trees by default. If an execution strategy relies on runfile # symlink tree, the tree is created on-demand. See: https://github.com/bazelbuild/bazel/&gt; &gt; issues/6627 # and https://github.com/bazelbuild/bazel/commit/03246077f948f2790a83520e7dccc2625650e6df build --nobuild_runfile_links test --nobuild_runfile_links # https://bazel.build/reference/command-line-reference#flag--legacy_external_runfiles build --nolegacy_external_runfiles test --nolegacy_external_runfiles]]></summary></entry><entry><title type="html">Abusing GitHub as a PyPI server</title><link href="https://fzakaria.com/2024/01/15/abusing-github-as-a-pypi-server.html" rel="alternate" type="text/html" title="Abusing GitHub as a PyPI server" /><published>2024-01-15T04:02:00+00:00</published><updated>2024-01-15T04:02:00+00:00</updated><id>https://fzakaria.com/2024/01/15/abusing-github-as-a-pypi-server</id><content type="html" xml:base="https://fzakaria.com/2024/01/15/abusing-github-as-a-pypi-server.html"><![CDATA[<blockquote>
  <p>I did not discover or invent this trick .</p>
</blockquote>

<p>I wanted to make available a Python wheel to some developers but I did not want to publish it on PyPI for a variety of reasons.</p>
<ol>
  <li>I am not the original author of the code and I did not want to take credit for it.</li>
  <li>I wanted to include the git commit hash in the version number which PyPI does not allow.</li>
</ol>

<!--more-->

<p>The <strong>trick</strong> is pretty simple but leverages two simple facts:</p>
<ol>
  <li>
    <p>For a URL to behave similar to PyPI for <code class="language-plaintext highlighter-rouge">pip</code> to install a package, it merely must provide an <code class="language-plaintext highlighter-rouge">index.html</code> file with links to the wheels.
This is the premise of <a href="https://peps.python.org/pep-0503/">PEP 503</a> which defines the PyPI Simple Repository API.</p>

    <div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="cp">&lt;!DOCTYPE html&gt;</span>
 <span class="nt">&lt;html&gt;</span>
 <span class="nt">&lt;body&gt;</span>
     <span class="nt">&lt;a</span> <span class="na">href=</span><span class="s">"/frob/"</span><span class="nt">&gt;</span>frob<span class="nt">&lt;/a&gt;</span>
     <span class="nt">&lt;a</span> <span class="na">href=</span><span class="s">"/spamspamspam/"</span><span class="nt">&gt;</span>spamspamspam<span class="nt">&lt;/a&gt;</span>
 <span class="nt">&lt;/body&gt;</span>
 <span class="nt">&lt;/html&gt;</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>GitHub Release Page has a view that includes all the links to all assets in the release. For instance for let’s consider the <a href="https://github.com/makslevental/mlir-wheels">mlir-wheels</a> repository that uses this trick. It has a <strong>single</strong> release with over 5,000 “assets”, where each asset is a wheel for a different version and particular platform.</p>
  </li>
</ol>

<p><img src="/assets/images/mlir_wheels_github_release.png" alt="mlir-wheels release page" /></p>

<p><code class="language-plaintext highlighter-rouge">pip</code> itself cannot use this page unfortunately, because the hyperlinks are loaded via Javascript.</p>

<p>There is however an <em>alternative</em> page that is a basic HTML view of all the assets.</p>

<p><a href="https://github.com/makslevental/mlir-wheels/releases/tag/latest">https://github.com/makslevental/mlir-wheels/releases/tag/latest</a> -&gt; <a href="https://github.com/makslevental/mlir-wheels/releases/expanded_assets/latest">https://github.com/makslevental/mlir-wheels/releases/expanded_assets/latest</a></p>

<p><img src="/assets/images/mlir_wheels_github_expanded_assets.png" alt="mlir-wheels expanded assets page" /></p>

<p>With this page you can easily use <code class="language-plaintext highlighter-rouge">pip</code> to install and upgrade the packages.</p>

<div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="go">pip install mlir-python-bindings \
    -f https://github.com/makslevental/mlir-wheels/releases/expanded_assets/latest
</span></code></pre></div></div>

<p>Happy hosting. 🎉</p>]]></content><author><name></name></author><summary type="html"><![CDATA[I did not discover or invent this trick . I wanted to make available a Python wheel to some developers but I did not want to publish it on PyPI for a variety of reasons. I am not the original author of the code and I did not want to take credit for it. I wanted to include the git commit hash in the version number which PyPI does not allow.]]></summary></entry><entry><title type="html">Quick insights using sqlelf</title><link href="https://fzakaria.com/2023/09/11/quick-insights-using-sqlelf.html" rel="alternate" type="text/html" title="Quick insights using sqlelf" /><published>2023-09-11T21:12:00+00:00</published><updated>2023-09-11T21:12:00+00:00</updated><id>https://fzakaria.com/2023/09/11/quick-insights-using-sqlelf</id><content type="html" xml:base="https://fzakaria.com/2023/09/11/quick-insights-using-sqlelf.html"><![CDATA[<blockquote>
  <p>Please checkout the <a href="https://github.com/fzakaria/sqlelf">sqlelf</a> repository and give me your feedback.</p>
</blockquote>

<p>I wrote in my earlier <a href="/2023/03/19/sqlelf-and-20-years-of-nix.html">post</a> about releasing <a href="https://github.com/fzakaria/sqlelf">sqlelf</a>.
I had the hunch that the existing tooling we have to interface with our object formats, such as ELF, are antiquated and ill specified.</p>

<p>Declarative languages, such as SQL, are such a wonderful abstraction to articulate over data layouts and let you reason about <em>what you want</em> rather than <em>how to get it</em>.</p>

<p>Since continuing to noodle 👨‍💻 on the project, I’ve been pleasantly surprised at some of the introspection, specifically with respect to symbols, I’ve been able
to accomplish with SQL that would have been a pain using traditional tools.</p>

<!--more-->

<p>Come on a stroll with me on a few case studies I’ve gone through on how SQL guided analysis wins out.</p>

<h2 id="symbol-resolution">Symbol Resolution</h2>

<p>One of the primary data structures within the ELF file is the symbol table, especially the <em>dynamic symbol table</em> that allows the use of shared objects (libraries).</p>

<p>A typical question someone may ask themselves though is:</p>

<p><strong>Which library that I load is providing <em>function foo</em>?</strong></p>

<p>This is a worthwhile question because you would like to know which shared object is not only providing the symbol definition but also which the linker (<code class="language-plaintext highlighter-rouge">ld.so</code>) choses
to link against at runtime.</p>

<p>The <em>state of the art</em> (prior to sqlelf) of how to retrieve this diagnostic information is using <code class="language-plaintext highlighter-rouge">LD_DEBUG</code> environment variable and trolling through the large dump of logs it emits. 🤦</p>

<div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="go">❯ LD_DEBUG=symbols,bindings /usr/bin/ruby |&amp; head
</span><span class="gp">   1228310:	symbol=__vdso_clock_gettime;</span><span class="w">  </span>lookup <span class="k">in </span><span class="nv">file</span><span class="o">=</span>linux-vdso.so.1 <span class="o">[</span>0]
<span class="go">   1228310:	binding file linux-vdso.so.1 [0] to linux-vdso.so.1 [0]: normal symbol `__vdso_clock_gettime' [LINUX_2.6]
</span><span class="gp">   1228310:	symbol=__vdso_gettimeofday;</span><span class="w">  </span>lookup <span class="k">in </span><span class="nv">file</span><span class="o">=</span>linux-vdso.so.1 <span class="o">[</span>0]
<span class="go">   1228310:	binding file linux-vdso.so.1 [0] to linux-vdso.so.1 [0]: normal symbol `__vdso_gettimeofday' [LINUX_2.6]
</span><span class="gp">   1228310:	symbol=__vdso_time;</span><span class="w">  </span>lookup <span class="k">in </span><span class="nv">file</span><span class="o">=</span>linux-vdso.so.1 <span class="o">[</span>0]
<span class="go">   1228310:	binding file linux-vdso.so.1 [0] to linux-vdso.so.1 [0]: normal symbol `__vdso_time' [LINUX_2.6]
</span><span class="gp">   1228310:	symbol=__vdso_getcpu;</span><span class="w">  </span>lookup <span class="k">in </span><span class="nv">file</span><span class="o">=</span>linux-vdso.so.1 <span class="o">[</span>0]
<span class="go">   1228310:	binding file linux-vdso.so.1 [0] to linux-vdso.so.1 [0]: normal symbol `__vdso_getcpu' [LINUX_2.6]
</span><span class="gp">   1228310:	symbol=__vdso_clock_getres;</span><span class="w">  </span>lookup <span class="k">in </span><span class="nv">file</span><span class="o">=</span>linux-vdso.so.1 <span class="o">[</span>0]
<span class="go">   1228310:	binding file linux-vdso.so.1 [0] to linux-vdso.so.1 [0]: normal symbol `__vdso_clock_getres' [LINUX_2.6]
</span></code></pre></div></div>

<p>Let’s see how we can re-think of this question as a declarative SQL statement:</p>

<pre><code class="language-SQL">SELECT caller.path as 'caller.path',
       callee.path as 'calee.path',
       caller.name,
       caller.demangled_name
FROM ELF_SYMBOLS caller
INNER JOIN ELF_SYMBOLS callee
ON
caller.name = callee.name AND
caller.path != callee.path AND
caller.imported = TRUE AND
callee.exported = TRUE
</code></pre>

<p>We can think of the above ask asking:</p>

<p><em>Please provide all pairings of symbols where the name is the same between any two different ELF files.
One of the files must export the symbol and the other must be importing it.</em></p>

<div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="go">❯ sqlelf /usr/bin/ruby --sql "SELECT caller.path as 'caller.path',
       callee.path as 'calee.path',
       caller.name
FROM ELF_SYMBOLS caller
INNER JOIN ELF_SYMBOLS callee
ON
caller.name = callee.name AND
caller.path != callee.path AND
caller.imported = TRUE AND
callee.exported = TRUE
LIMIT 10"
┌───────────────┬────────────────────────────────────────────────┬───────────────────┐
│  caller.path  │                   calee.path                   │       name        │
│ /usr/bin/ruby │ /usr/lib/x86_64-linux-gnu/libruby-3.1.so.3.1.2 │ ruby_run_node     │
│ /usr/bin/ruby │ /usr/lib/x86_64-linux-gnu/libruby-3.1.so.3.1.2 │ ruby_init         │
│ /usr/bin/ruby │ /usr/lib/x86_64-linux-gnu/libruby-3.1.so.3.1.2 │ ruby_options      │
│ /usr/bin/ruby │ /usr/lib/x86_64-linux-gnu/libruby-3.1.so.3.1.2 │ ruby_sysinit      │
│ /usr/bin/ruby │ /usr/lib/x86_64-linux-gnu/libc.so.6            │ __stack_chk_fail  │
│ /usr/bin/ruby │ /usr/lib/x86_64-linux-gnu/libruby-3.1.so.3.1.2 │ ruby_init_stack   │
│ /usr/bin/ruby │ /usr/lib/x86_64-linux-gnu/libc.so.6            │ setlocale         │
│ /usr/bin/ruby │ /usr/lib/x86_64-linux-gnu/libc.so.6            │ __libc_start_main │
│ /usr/bin/ruby │ /usr/lib/x86_64-linux-gnu/libc.so.6            │ __libc_start_main │
│ /usr/bin/ruby │ /usr/lib/x86_64-linux-gnu/libc.so.6            │ __cxa_finalize    │
└───────────────┴────────────────────────────────────────────────┴───────────────────┘
</span></code></pre></div></div>

<p>🥳 That sure beats dealing with unstructured text. sqlelf can also emit a myriad of output formats such as csv or json.</p>

<h2 id="symbol-shadowing">Symbol Shadowing</h2>

<p>The reality of <code class="language-plaintext highlighter-rouge">LD_DEBUG</code> however is that it usefulness came in knowing the <em>final resolution</em> of a given symbol.</p>

<p>ELF files are free to have and export any symbols and often times, wether by accident (benign) or maliciously, the same symbol may be exported by
multiple libraries.</p>

<blockquote>
  <p>This is what empowers tools such as <code class="language-plaintext highlighter-rouge">LD_PRELOAD</code> so that users can take over symbols such as <code class="language-plaintext highlighter-rouge">malloc</code> and replace them with alternative strategies.</p>
</blockquote>

<p>The linker, according to the <a href="https://refspecs.linuxbase.org/elf/gabi4+/ch5.dynamic.html">SystemV ABI</a>, examines the symbol tables with a breadth-first search across the dependency graph of the shared object libraries.</p>

<p>A typical question someone may ask themselves though is:</p>

<p><strong>What symbols are currently shadowed in my dependency graph?</strong></p>

<p>Let’s see how we can re-think of this question as a declarative SQL statement:</p>

<pre><code class="language-SQL">SELECT name, version, count(*) as symbol_count,
       GROUP_CONCAT(path, ':') as libraries
FROM elf_symbols
WHERE exported = TRUE
GROUP BY name, version
HAVING count(*) &gt;= 2
</code></pre>

<p>We can think of the above ask asking:</p>

<p><em>Please provide me all symbols (and the library that defines them) that are exported by more than 2 libraries</em>.</p>

<p>Any symbol here is technically being shadowed, whether on purpose or benign.</p>

<p>Revisiting the same <em>ruby</em> example above we can see the results.</p>
<div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="go">❯ sqlelf /usr/bin/ruby --recursive --sql "
SELECT name, version, count(*) as symbol_count,
       GROUP_CONCAT(path, ':') as libraries
FROM elf_symbols
WHERE exported = TRUE
GROUP BY name, version
</span><span class="gp">HAVING count(*) &gt;</span><span class="o">=</span> 2<span class="s2">"
</span><span class="go">┌────────────┬─────────────┬──────────────┬─────────────────────────────────────────────────────────────────────────┐
│    name    │   version   │ symbol_count │                                libraries                                │
│ __finite   │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ __finitef  │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ __finitel  │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ __signbit  │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ __signbitf │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ __signbitl │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ copysign   │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ copysignf  │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ copysignl  │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ finite     │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ finitef    │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ finitel    │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ frexp      │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ frexpf     │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ frexpl     │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ ldexp      │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ ldexpf     │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ ldexpl     │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ modf       │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ modff      │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ modfl      │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ scalbn     │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ scalbnf    │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
│ scalbnl    │ GLIBC_2.2.5 │ 2            │ /usr/lib/x86_64-linux-gnu/libm.so.6:/usr/lib/x86_64-linux-gnu/libc.so.6 │
└────────────┴─────────────┴──────────────┴─────────────────────────────────────────────────────────────────────────┘
</span></code></pre></div></div>

<p>In this particular case, there is no malicious symbol shadowing and the symbols shadowed by <code class="language-plaintext highlighter-rouge">libm</code> and <code class="language-plaintext highlighter-rouge">libc</code> are well
known. In fact, on many systems, <code class="language-plaintext highlighter-rouge">libm</code> is a symlink to <code class="language-plaintext highlighter-rouge">libc</code>.</p>

<p>I have <a href="https://arxiv.org/abs/2211.05118">previously written</a> about through the development of <a href="https://github.com/fzakaria/shrinkwrap">shrinkwrap</a> that a more
annoying shadowing can happen with OpenMPI. It’s pretty easy to accidentally get the <em>no-op</em> library implementation earlier in
breadth-first search and find yourself with a sequential application.</p>

<p>I’ve included a neat <a href="https://github.com/fzakaria/sqlelf/blob/main/examples/shadowed-symbols/Makefile">example</a> in the sqlelf repository that you can play with
to test shadowing symbols and see the results of sqlelf. 🕵️</p>

<p>If you find any of this fascinating, contribute and let’s work to make accessing ELF via SQL simple and productive.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Please checkout the sqlelf repository and give me your feedback. I wrote in my earlier post about releasing sqlelf. I had the hunch that the existing tooling we have to interface with our object formats, such as ELF, are antiquated and ill specified. Declarative languages, such as SQL, are such a wonderful abstraction to articulate over data layouts and let you reason about what you want rather than how to get it. Since continuing to noodle 👨‍💻 on the project, I’ve been pleasantly surprised at some of the introspection, specifically with respect to symbols, I’ve been able to accomplish with SQL that would have been a pain using traditional tools.]]></summary></entry><entry><title type="html">sqlelf and 20 years of Nix</title><link href="https://fzakaria.com/2023/03/19/sqlelf-and-20-years-of-nix.html" rel="alternate" type="text/html" title="sqlelf and 20 years of Nix" /><published>2023-03-19T20:48:00+00:00</published><updated>2023-03-19T20:48:00+00:00</updated><id>https://fzakaria.com/2023/03/19/sqlelf-and-20-years-of-nix</id><content type="html" xml:base="https://fzakaria.com/2023/03/19/sqlelf-and-20-years-of-nix.html"><![CDATA[<blockquote>
  <p>If you want to skip ahead, please checkout the <a href="https://github.com/fzakaria/sqlelf">sqlelf</a> repository and give me your feedback.</p>
</blockquote>

<p>🎉 We are celebrating 20 years of <a href="https://nixos.org">Nix</a> 🎉</p>

<p>Within that 20 years Nix has ushered in a new paradigm of how to build software reliably that is becoming more ubiquitous in the software industry.
It has inspired imitators such as <a href="https://spack.io/">Spack</a> &amp; <a href="https://guix.gnu.org/">Guix</a>.</p>

<p>Given the concepts introduced by Nix and it’s willingnes to eschew some fundamental Linux concepts such as the Filesystem Hierarchy Standard.</p>

<p>I can’t help but think <em>has Nix gone far enough within the 20 years?</em></p>

<!--more-->

<p>If you have kept an eye on some of the work I’ve been doing and thinking of, I have spent some time thinking how Nix can make further progress on goals of reliability and reproducibility.</p>

<blockquote>
  <p>If you haven’t seen my talk on <a href="https://www.youtube.com/watch?v=HZKFe4mCkr4">Rethinking basic primitives for store based systems</a>, I recommend you watch it.</p>
</blockquote>

<p>Nix, and more specifically NixOS, is uniquely poised to do-away with many of the historic cruft that has plaque us in software due to the fact that it’s dependency closure goes down to the Linux kernel!</p>

<p>There is no short-list of components we can re-imagine however I have been focused on the dynamic linker / interpreter. Concepts of the *Unixes of the world which are largely historic are up for grabs.</p>

<p>As part of my work on <a href="https://github.com/fzakaria/shrinkwrap">Shrinkwrap</a>, I was getting pretty frustrated working with the ELF file format.</p>

<blockquote>
  <p>Checkout my SuperComputing 2022 paper <a href="https://arxiv.org/abs/2211.05118">Mapping Out the HPC Dependency Chaos</a></p>
</blockquote>

<p>The best tools we have to introspect binaries are <code class="language-plaintext highlighter-rouge">readelf</code> and <code class="language-plaintext highlighter-rouge">objdump</code> whom simply dump raw ASCII text to the console.</p>
<div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="go">❯ readelf --demangle --dyn-syms /usr/bin/ruby | head

Symbol table '.dynsym' contains 22 entries:
   Num:    Value          Size Type    Bind   Vis      Ndx Name
     0: 0000000000000000     0 NOTYPE  LOCAL  DEFAULT  UND 
     1: 0000000000000000     0 FUNC    GLOBAL DEFAULT  UND ruby_run_node
</span></code></pre></div></div>

<p><em>Why are we restricted to such a challenging file format to work with?</em></p>

<p>I am working on an idea which I am very excited about 🤓 which I will be writing about.
To prove to myself that the idea has merit, I wanted to explore making a tool that allows for easier introspection of ELF files.</p>

<p>Using the power of <a href="https://sqlite.org/index.html">SQLite</a> and the Virtual Tables concept, I wrote <a href="https://github.com/fzakaria/sqlelf">sqlelf</a>: <em>Explore ELF objects through the power of SQL.</em></p>

<div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="go">❯ sqlelf /usr/bin/ruby /bin/ls /usr/bin/pnmarith
</span><span class="gp">sqlite&gt;</span><span class="w"> </span>SELECT elf_headers.path, COUNT<span class="o">(</span><span class="k">*</span><span class="o">)</span> as num_sections
<span class="gp">    ..&gt;</span><span class="w"> </span>FROM elf_headers
<span class="gp">    ..&gt;</span><span class="w"> </span>INNER JOIN elf_sections ON elf_headers.path <span class="o">=</span> elf_sections.path
<span class="gp">    ..&gt;</span><span class="w"> </span>WHERE elf_headers.type <span class="o">=</span> 3
<span class="gp">    ..&gt;</span><span class="w"> </span>GROUP BY elf_headers.path<span class="p">;</span>
<span class="go">path|num_sections
/bin/ls|31
/usr/bin/pnmarith|27
/usr/bin/ruby|28
</span></code></pre></div></div>

<p>If I can prove a clean 1:1 mapping between the two formats then there is an amazing room for potential.</p>

<p>I am still working through the domain model mapping to individual tables (<em>contributions and help appreciated!</em>) but I am really excited by this idea.</p>
<ol>
  <li>Linker specifications can be articulated with SQL to guarantee semantics.</li>
  <li>Dynamic loader specifications can also be defined with SQL + potentially use of ACID constraints.</li>
  <li>Analysis of files can be done at large easily using SQL.</li>
  <li>We remove a custom file format.</li>
</ol>

<p>Unix introduced the simple concept of <em>what if everything was a file?</em></p>

<p>🙈 🙉 🙊 <strong>What if everything was a database?</strong> 🙈 🙉 🙊</p>]]></content><author><name></name></author><summary type="html"><![CDATA[If you want to skip ahead, please checkout the sqlelf repository and give me your feedback. 🎉 We are celebrating 20 years of Nix 🎉 Within that 20 years Nix has ushered in a new paradigm of how to build software reliably that is becoming more ubiquitous in the software industry. It has inspired imitators such as Spack &amp; Guix. Given the concepts introduced by Nix and it’s willingnes to eschew some fundamental Linux concepts such as the Filesystem Hierarchy Standard. I can’t help but think has Nix gone far enough within the 20 years?]]></summary></entry><entry><title type="html">Making RUNPATH redundant for Nix</title><link href="https://fzakaria.com/2022/09/12/making-runpath-redundant-for-nix.html" rel="alternate" type="text/html" title="Making RUNPATH redundant for Nix" /><published>2022-09-12T16:22:00+00:00</published><updated>2022-09-12T16:22:00+00:00</updated><id>https://fzakaria.com/2022/09/12/making-runpath-redundant-for-nix</id><content type="html" xml:base="https://fzakaria.com/2022/09/12/making-runpath-redundant-for-nix.html"><![CDATA[<blockquote>
  <p>This post is a direct translation of Harmen Stoppel’s <a href="https://stoppels.ch/2022/08/04/stop-searching-for-shared-libraries.html">blog</a> on the same subject for Nix. He has also contributed a fix to <a href="http://spack.io/">Spack</a>.</p>

  <p>Please check out <a href="https://github.com/fzakaria/nix-harden-needed">https://github.com/fzakaria/nix-harden-needed</a> for my solution for the Nix ecosystem.</p>
</blockquote>

<p>Nix and other store-like systems (i.e. Guix or Spack), resolve all their dependencies within their store (<em>/nix/store</em>) to enforce hermiticity. They leverage for the most part <em>RUNPATH</em> which is a field on the ELF executable to instruct the
dynamic linker where to discover the libraries – as opposed to searching default search paths like <em>/lib</em>.</p>

<div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="go">❯ which ruby
/nix/store/8k4sgk3bmxnj0jvcgc4wvyd8ilg0ww3y-ruby-2.7.6/bin/ruby

</span><span class="gp">❯ patchelf --print-rpath $</span><span class="o">(</span>which ruby<span class="o">)</span>
<span class="go">/nix/store/8k4sgk3bmxnj0jvcgc4wvyd8ilg0ww3y-ruby-2.7.6/lib:/nix/store/r90cncsaa519pwqpijg7ii4rkcmwjn6h-zlib-1.2.12/lib:/nix/store/bvy2z17rzlvkx2sj7fy99ajm853yv898-glibc-2.34-210/lib
</span></code></pre></div></div>

<p>I have a paper about to published for <a href="https://sc22.supercomputing.org/">SuperComputing 2022</a> (please reach out if you’d like early copy) that demonstrates there is a non-trivial cost to continuously searching needlessly through
the <em>RUNPATH</em>. In fact, I have <a href="/2022/03/15/shrinkwrap-taming-dynamic-shared-objects.html">written previously</a> about the specific costs and our tool <a href="https://github.com/fzakaria/shrinkwrap">Shrinkwrap</a> that can avoid it.</p>

<p>Although Shrinkwrap is one approach for a solution, it is merely a bandaid over the existing problem.</p>

<p>Can systems like Nix do more to solve this problem?
<!--more--></p>

<p>Nix and similar systems have the benefit of knowing ahead of time the exact path for every dependency. They are also particularly well positioned to make deep impactful changes because they rebuild the world <strong>bottom up</strong>, starting from libc.</p>

<p>Although these systems are radical departures from traditional Linux distributions and try to rebel against the Filesystem Hierachy Standard, they nevertheless rely on fundamental tooling and control knobs such as <em>RUNPATH</em> to bandaid over the solution.</p>

<p>🧐 Let’s make the use of <em>RUNPATH</em> in Nix obsolete and unnecessary.</p>

<p>We can do this through the observation that GCC will propagate the <em>soname</em> stored in the library into the <em>DT_NEEDED</em> entry of the upstream binary. That means that if our <em>soname</em> happens to be an absolute path, such as a <em>/nix/store</em> entry, it will get set for the <em>DT_NEEDED</em> and searching through <em>RUNPATH</em> will be unnecessary. 🎆</p>

<p>Here is a small example to demonstrate</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># Let's build our library with an absolute path for soname
# notice I have set the soname to an absoulte path
❯ gcc -shared -o libf.so -Wl,-soname,/nix/store/znxycsxlnx2s9zn6g0s0fl4z57ar7aps-libf-0.1/lib/libf.so -x c - &lt;&lt;EOF
        #include &lt;stdio.h&gt;
        void f() { puts("hello world"); }
    EOF

❯ patchelf --print-soname libf.so
/nix/store/zir4jfm86i3037lnsaz5br55iwavvhpz-libf-0.1/lib/libf.so

# now build the application that relies on it
❯ gcc -o app -lf -L. -x c - &lt;&lt;EOF
    void f();
    int main() { f(); }
EOF

❯ patchelf --print-needed app
/nix/store/znxycsxlnx2s9zn6g0s0fl4z57ar7aps-libf-0.1/lib/libf.so
libc.so.6
</code></pre></div></div>

<p>Nothing of the above cannot be done during the building of a library <strong>automatically</strong> within Nix and specifically Nixpkgs. During build time, the store path is known (<em>$out</em>) and it can be set for the <em>soname</em>.</p>

<p>Ideally, I believe a deep fix for Nixpkgs is possible by altering the <a href="https://github.com/NixOS/nixpkgs/blob/master/pkgs/build-support/bintools-wrapper/ld-wrapper.sh">ld-wrapper</a> to correctly set the <em>soname</em>
which will be picked up for every derivation built with <em>stdenv</em>.</p>

<p>Unfortunately since I am not a <em>bash</em> Guru, I opted for a fix similar to <em>autopatchelf</em> – <a href="https://github.com/fzakaria/nix-harden-needed">nix-harden-needed</a>.</p>

<p>It is a Nix <em>stdenv</em> setuphook that will automatically call <em>patchelf</em> to set the <em>soname</em> to the correct value – their absolute path. This means that the <em>DT_NEEDED</em>
entry for any binary upstream that relies on it will leverage the absolute path and avoid the costly lookup through <em>RUNPATH</em>.</p>

<p>It’s incredibly easy to use, you simply have to add it as a build input to your derivation. Let’s revisit the above example using the setup-hook.</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>let libf = stdenv.mkDerivation rec {
  pname = "libf";
  version = "0.1";

  dontUnpack = true;

  buildInputs = [
    nix-harden-needed-hook
  ];

  buildPhase = ''
    # Enable if you'd like to see wrapper debug information
    # NIX_DEBUG=1 
    $CC -shared -o libf.so -Wl,-soname,libf.so -x c - &lt;&lt;EOF
        #include &lt;stdio.h&gt;
        void f() { puts("hello world"); }
    EOF
  '';

  installPhase = ''
    mkdir -p $out/lib
    mv libf.so $out/lib
  '';
};
in
stdenv.mkDerivation rec {
  pname = "app";

  version = "0.1";

  dontUnpack = true;

  buildInputs = [
    libf
  ];

  buildPhase = ''
    # Enable if you'd like to see wrapper debug information
    # NIX_DEBUG=1 
    $CC -o app -lf -x c - &lt;&lt;EOF
        void f();
        int main() { f(); }
    EOF
  '';

  installPhase = ''
    mkdir -p $out/bin
    mv app $out/bin
  '';

}
</code></pre></div></div>

<p>The built binary will correctly have the <em>DT_NEEDED</em> set to the absolute path of the shared object file.</p>
<div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="go">❯ patchelf --print-needed /nix/store/6pg9d3lwlmgcmmswv937fcy211vkqxch-app-0.1/bin/app
/nix/store/znxycsxlnx2s9zn6g0s0fl4z57ar7aps-libf-0.1/lib/libf.so
libc.so.6
❯ patchelf --print-soname /nix/store/zir4jfm86i3037lnsaz5br55iwavvhpz-libf-0.1/lib/libf.so
/nix/store/zir4jfm86i3037lnsaz5br55iwavvhpz-libf-0.1/lib/libf.so
</span></code></pre></div></div>

<p>I believe systems such as Nix have ushered a new paradigm shift of thinking about software and there is immense opportunity to go beyond the current limitations and tooling.</p>

<p>🙇 Everything can be re-thought and re-imagined.</p>

<blockquote>
  <p>Please reach out if you are a <em>bash guru</em> and we can work together to apply a deeper fix to Nixpkgs of the above ideas.</p>
</blockquote>]]></content><author><name></name></author><summary type="html"><![CDATA[This post is a direct translation of Harmen Stoppel’s blog on the same subject for Nix. He has also contributed a fix to Spack. Please check out https://github.com/fzakaria/nix-harden-needed for my solution for the Nix ecosystem. Nix and other store-like systems (i.e. Guix or Spack), resolve all their dependencies within their store (/nix/store) to enforce hermiticity. They leverage for the most part RUNPATH which is a field on the ELF executable to instruct the dynamic linker where to discover the libraries – as opposed to searching default search paths like /lib. ❯ which ruby /nix/store/8k4sgk3bmxnj0jvcgc4wvyd8ilg0ww3y-ruby-2.7.6/bin/ruby ❯ patchelf --print-rpath $(which ruby) /nix/store/8k4sgk3bmxnj0jvcgc4wvyd8ilg0ww3y-ruby-2.7.6/lib:/nix/store/r90cncsaa519pwqpijg7ii4rkcmwjn6h-zlib-1.2.12/lib:/nix/store/bvy2z17rzlvkx2sj7fy99ajm853yv898-glibc-2.34-210/lib I have a paper about to published for SuperComputing 2022 (please reach out if you’d like early copy) that demonstrates there is a non-trivial cost to continuously searching needlessly through the RUNPATH. In fact, I have written previously about the specific costs and our tool Shrinkwrap that can avoid it. Although Shrinkwrap is one approach for a solution, it is merely a bandaid over the existing problem. Can systems like Nix do more to solve this problem?]]></summary></entry><entry><title type="html">Shrinkwrap: Taming dynamic shared objects</title><link href="https://fzakaria.com/2022/03/15/shrinkwrap-taming-dynamic-shared-objects.html" rel="alternate" type="text/html" title="Shrinkwrap: Taming dynamic shared objects" /><published>2022-03-15T01:53:00+00:00</published><updated>2022-03-15T01:53:00+00:00</updated><id>https://fzakaria.com/2022/03/15/shrinkwrap-taming-dynamic-shared-objects</id><content type="html" xml:base="https://fzakaria.com/2022/03/15/shrinkwrap-taming-dynamic-shared-objects.html"><![CDATA[<blockquote>
  <p>This is a blog post of a paper I have submitted for a <a href="https://catalog.ucsc.edu/en/Current/General-Catalog/Courses/CSE-Computer-Science-and-Engineering/Graduate/CSE-215">UCSC course</a> project.</p>

  <p>If you are interested in the code check out <a href="https://github.com/fzakaria/shrinkwrap">https://github.com/fzakaria/shrinkwrap</a></p>
</blockquote>

<p>One of the fundamental data management units within a Linux system are the shared object files that are loaded into memory by dynamically linked
processes at startup. The mechanism and approach to which dynamic linking is done has <strong>not changed</strong> since it’s inception however software has become
increasingly complex.</p>

<!--more-->

<p><em>This is the full build and run closure for Ruby in Nix, which is a good visual depiction of the complexity.</em>
<img src="/assets/images/ruby_full_closure.png" alt="Ruby closure" /></p>

<p>The discovery of the needed dependencies can at most be controlled by small set of directory lists or typically rely on convention for discovery, better known as the <a href="https://en.wikipedia.org/wiki/Filesystem_Hierarchy_Standard">Filesystem Hierarchy Standard (FHS)</a>.</p>

<p>The reliance on convention for discovery of shared objects while simple, has resulted in challenges when trying to rebuild solutions reproducibly and
root cause discrepancies between machines – <em>“It works on my machine but not yours”</em>.</p>

<p>Novel new software packaging models have emerged such as <a href="https://nixos.org/">Nix</a>, <a href="https://guix.gnu.org/">Guix</a> and <a href="https://spack.io/">Spack</a>, that attempt to tame the chaos of dependency hell by eschewing all uses of the FHS and relying on explicit deterministic paths. These tools have made great strides in moving software packaging to becoming more reproducible but still exhibit certain flaws, specifically
performance, as a result of building upon tooling that was designed for a different paradigm.</p>

<p><a href="https://github.com/fzakaria/shrinkwrap">Shrinkwrap</a> is a tool that attempts to overcome some of the performance limitations with how software may be packed in store-like models by <strong>freezing the dependencies</strong> directly on the executable.</p>

<blockquote>
  <p>For a different approach to this problem, check out <a href="https://guix.gnu.org/blog/2021/taming-the-stat-storm-with-a-loader-cache/">this blog post</a> by the Guix developers.</p>
</blockquote>

<p>The number of dependencies needed for a particular binary transitively, and the <code class="language-plaintext highlighter-rouge">RUNPATH</code> can vary greatly. For instance, <em>emacs</em> lists 36 directories in it’s <code class="language-plaintext highlighter-rouge">RUNPATH</code> and requires 103 dependencies to be resolved.</p>

<p>The result is that the dynamic linker must attempt potentially 3600 filesystem operations (openat or stat) to resolve the needed dependencies <strong>every time the process is started</strong>.</p>

<p>🐌 This exorbitant cost can be made worse if the store itself resides on a shared filesystem such as NFS. 🐌</p>

<div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>patchelf <span class="nt">--print-rpath</span> /nix/store/vvxcs4f8x14gyahw50ssff3sk2dij2b3-emacs-27.2/bin/.emacs-27.2-wrapped <span class="se">\</span>
<span class="go">    | tr ':' '\n' | wc -l
36

</span><span class="gp">$</span><span class="w"> </span>ldd /nix/store/vvxcs4f8x14gyahw50ssff3sk2dij2b3-emacs-27.2/bin/.emacs-27.2-wrapped | <span class="nb">wc</span> <span class="nt">-l</span>
<span class="go">103
</span></code></pre></div></div>

<p>💡 When faced with a recurring problem, often the solution is to cache the previous answer to avoid unecessary work.</p>

<p>Shrinkwrap adopts this approach by freezing the required dependencies directly into the <code class="language-plaintext highlighter-rouge">DT_NEEDED</code> section of the binary by having it point to an absolute path. The
transitive dependency list is also lifted to the top-level binary to simplify auditing the required dependencies.</p>

<div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>patchelf <span class="nt">--print-needed</span> /nix/store/zb2h75vbhg7w42b3f42bl0y2d4m0a4n3-emacs-27.1/bin/.emacs-27.1-wrapped
<span class="go">libtiff.so.5
libjpeg.so.62
libpng16.so.16
libz.so.1
libungif.so.4
libXpm.so.4
libgtk-3.so.0
libgdk-3.so.0

</span><span class="gp">$</span><span class="w"> </span>shrinkwrap /nix/store/zb2h75vbhg7w42b3f42bl0y2d4m0a4n3-emacs-27.1/bin/.emacs-27.1-wrapped <span class="nt">-o</span> emacs_stamped
<span class="go">
</span><span class="gp">$</span><span class="w"> </span>patchelf <span class="nt">--print-needed</span> emacs_stamped
<span class="go">/nix/store/2nkjrh3za68vrw6kf8lxn6nq1dval05v-gcc-10.3.0-lib/lib/libstdc++.so.6
/nix/store/jvbyjnjh4w8qg7izfq4x5d2wy9lv9461-icu4c-70.1/lib/libicudata.so.70
/nix/store/2kzsm8hhc4lzji6g1ksav9bdjbbiyxln-libgpg-error-1.42/lib/libgpg-error.so.0
/nix/store/mpwncqr8fbqflmglkrxj7a288xdbymk3-util-linux-2.37.2-lib/lib/libblkid.so.1
/nix/store/8n6mjngkw6909rx631rzwby2rsdk0blf-libglvnd-1.3.4/lib/libGLX.so.0
/nix/store/8n6mjngkw6909rx631rzwby2rsdk0blf-libglvnd-1.3.4/lib/libGLdispatch.so.0
/nix/store/xlvnyyviqcjys8if5hgkyykgv7d10hb8-libdatrie-2019-12-20-lib/lib/libdatrie.so.1
/nix/store/2zl3dw54ysdf55hngapkkfhiw0w8c9gp-json-glib-1.6.6/lib/libjson-glib-1.0.so.0
/nix/store/30q5xa4pfbvic54nh68qn86w6kjki66i-sqlite-3.36.0/lib/libsqlite3.so.0
/nix/store/jvbyjnjh4w8qg7izfq4x5d2wy9lv9461-icu4c-70.1/lib/libicui18n.so.70
/nix/store/jvbyjnjh4w8qg7izfq4x5d2wy9lv9461-icu4c-70.1/lib/libicuuc.so.70
</span></code></pre></div></div>

<p>Applying Shrinkwrap resulted in a large reduction in syscalls, which equates to a <strong>36x speedup</strong>. The absolute amount
recovered may seem negligible however this unecessary penalty is paid on every process invocation, and on every machine executing the binary.</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Program</th>
      <th style="text-align: center">Calls(stat/openat)</th>
      <th style="text-align: center">Time (Seconds)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">emacs</td>
      <td style="text-align: center">1823</td>
      <td style="text-align: center">0.034121</td>
    </tr>
    <tr>
      <td style="text-align: center">emacs_stamped</td>
      <td style="text-align: center">104</td>
      <td style="text-align: center">0.000950</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>The above was captured using <code class="language-plaintext highlighter-rouge">strace – strace -e openat,stat -c ./emacs_stamped --version</code></p>
</blockquote>

<p>Shrinkwrap relies on the ability for a dynamic linker to deduplicate libraries with a common basename or whose soname (ELF header value) are the same. For instance in the below image, Shrinkwrap elevated <code class="language-plaintext highlighter-rouge">libac.so</code> to a direct absolute dependency of the binary, but relies on the dynamic linker deduplicating the resolution for <code class="language-plaintext highlighter-rouge">libxyz.so</code> which does not refer to it absolutely.</p>

<p><img src="/assets/images/glibc_dedupe.png" alt="glibc dedupe" /></p>

<p>⚠️ This functionality currently does not exist in <em>musl</em> and only works with <em>glibc</em>. ⚠️</p>

<blockquote>
  <p>Please see <a href="https://www.openwall.com/lists/musl/2021/12/21/1">this mailing list discussion</a> for more details with <em>musl</em>.</p>
</blockquote>

<p>Nothing in Shrinkwrap assumes any Nix specifics and it may also be integrated into other store-like systems as well such as Guix and Spack.</p>

<p>It is not yet integrated into Nixpkgs but I woud love feedback. 😊</p>

<h2 id="philosophical-questions">Philosophical Questions</h2>

<p>Changing the needed dynamic dependencies to point to absolute paths, especially when those paths are immutable and content-addressable, may
have philosophical and legal considerations for certain open-source licenses such as <em>LGPL</em>.</p>

<p>LGPL specifically mentions that only in the case of dynamic linking is the license not propagated over. Although these dependencies go through the process of being dynamically linked, the library they are linked
to is effectively fixed.</p>

<p><em>Does this distinction blur the differentiation between static and dynamic linking?</em></p>

<p><em>What if the linker validated the content-address to also verify the library hasn’t been changed?</em></p>

<p>Additional investigation into the legal ramifications may be an opportunity for future work.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[This is a blog post of a paper I have submitted for a UCSC course project. If you are interested in the code check out https://github.com/fzakaria/shrinkwrap One of the fundamental data management units within a Linux system are the shared object files that are loaded into memory by dynamically linked processes at startup. The mechanism and approach to which dynamic linking is done has not changed since it’s inception however software has become increasingly complex.]]></summary></entry><entry><title type="html">Computing all output paths for every attribute in Nixpkgs</title><link href="https://fzakaria.com/2022/01/06/computing-all-output-paths-for-every-attribute-in-nixpkgs.html" rel="alternate" type="text/html" title="Computing all output paths for every attribute in Nixpkgs" /><published>2022-01-06T04:49:00+00:00</published><updated>2022-01-06T04:49:00+00:00</updated><id>https://fzakaria.com/2022/01/06/computing-all-output-paths-for-every-attribute-in-nixpkgs</id><content type="html" xml:base="https://fzakaria.com/2022/01/06/computing-all-output-paths-for-every-attribute-in-nixpkgs.html"><![CDATA[<p>Nix is an amazing tool, unfortunately doing <em>simple</em> things can be quite challenging.</p>

<p>This is a little write-up of my attempt to try and accomplish what I would have thought to be a <em>simple</em> thing; computing all store paths for every attribute in <a href="https://github.com/NixOS/nixpkgs/">nixpkgs</a>.</p>

<p>Why would I want to do such a thing?</p>

<p>I had some <em>/nix/store</em> entries on my system and I wanted to revisit the <strong>exact</strong> nixpkgs commit with which it was built to debug something.
Without this <a href="https://en.wikipedia.org/wiki/Reverse_index">reverse index</a> you are pretty much out of luck for figuring it out.</p>

<blockquote>
  <p>I want to give early <em>shoutout</em> to other similar tools in this space that let you do meta searches over nixpkgs such as <a href="https://lazamar.co.uk/nix-versions/">Nix Package Versions</a> and <a href="https://pkgs.on-nix.com/">Pkgs on Nix</a>.</p>
</blockquote>

<!--more-->

<p>🗣️ This goal has been made extra arduous due to the migrations of the new Nix CLI commands and the migration to Flakes.</p>

<h2 id="-goal-1-get-a-list-of-all-package-names">🎯 Goal 1: Get a list of all package names</h2>

<p>Any attempt to try and do this within the Nix language expression is <strong>doomed</strong>.
Any immediate attempt to iterate over all keys within the <em>top-level</em> attribute in Nixpkgs is faced with hurdle and after hurdle.</p>

<p>I got pretty far with the following <a href="https://gist.github.com/fzakaria/ae27a2ad58e1d80f5453b0ed0052297f">gist</a> but it still hit roadblocks.</p>
<div class="language-nix highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">let</span>
  <span class="nv">pkgs</span> <span class="o">=</span> <span class="kr">import</span> <span class="o">&lt;</span><span class="nv">nixpkgs</span><span class="o">&gt;</span> <span class="p">{</span>
    <span class="nv">config</span><span class="o">.</span><span class="nv">allowBroken</span> <span class="o">=</span> <span class="kc">true</span><span class="p">;</span>
    <span class="nv">config</span><span class="o">.</span><span class="nv">allowUnfree</span> <span class="o">=</span> <span class="kc">true</span><span class="p">;</span>
  <span class="p">};</span>
  <span class="nv">lib</span> <span class="o">=</span> <span class="kr">import</span> <span class="o">&lt;</span><span class="sx">nixpkgs/lib</span><span class="o">&gt;</span><span class="p">;</span>
  <span class="nv">tryEval</span> <span class="o">=</span> <span class="kr">builtins</span><span class="o">.</span><span class="nv">tryEval</span><span class="p">;</span>
<span class="kn">in</span>
  <span class="nv">lib</span><span class="o">.</span><span class="nv">mapAttrs</span> <span class="p">(</span><span class="nv">k</span><span class="p">:</span> <span class="nv">v</span><span class="p">:</span>
    <span class="kd">let</span> <span class="nv">name</span> <span class="o">=</span> <span class="p">(</span><span class="nv">tryEval</span> <span class="nv">v</span><span class="o">.</span><span class="nv">name</span> <span class="nv">or</span> <span class="s2">""</span><span class="p">);</span>
    <span class="nv">out</span> <span class="o">=</span> <span class="p">(</span><span class="nv">tryEval</span> <span class="nv">v</span><span class="o">.</span><span class="nv">outPath</span> <span class="nv">or</span> <span class="s2">""</span><span class="p">);</span>
    <span class="kn">in</span> <span class="p">{</span>
      <span class="nv">name</span> <span class="o">=</span> <span class="nv">name</span><span class="o">.</span><span class="nv">value</span><span class="p">;</span>
      <span class="nv">out</span>  <span class="o">=</span> <span class="nv">out</span><span class="o">.</span><span class="nv">value</span><span class="p">;</span>
    <span class="p">}</span>
  <span class="p">)</span> <span class="nv">pkgs</span>
</code></pre></div></div>

<p>Feedback from the community has been to use <em>nix</em> tooling such as <em>nix-env</em> or <em>nix-search</em> that have special handling for all these sharp edges, or some of the <em>fancier</em> work used
by <a href="https://github.com/NixOS/hydra">hydra</a> or <a href="https://github.com/NixOS/ofborg/">ofborg</a>.</p>

<p>I succumb to peer pressure and decided to use these tools 😮‍💨 rather than what I was hoping to be an elegant <em>pure Nix</em> expression.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>❯ nix search <span class="nb">.</span> <span class="nt">--json</span> | jq <span class="nt">-r</span> <span class="s1">'keys|.[]'</span> <span class="o">&gt;</span> package-names.txt

❯ <span class="nb">head</span> <span class="nt">-n</span> 10 package-names.txt
legacyPackages.x86_64-linux.AMB-plugins
legacyPackages.x86_64-linux.ArchiSteamFarm
legacyPackages.x86_64-linux.AusweisApp2
legacyPackages.x86_64-linux.CHOWTapeModel
legacyPackages.x86_64-linux.ChowKick
legacyPackages.x86_64-linux.CoinMP
legacyPackages.x86_64-linux.CuboCore.coreaction
legacyPackages.x86_64-linux.CuboCore.corearchiver
legacyPackages.x86_64-linux.CuboCore.corefm
legacyPackages.x86_64-linux.CuboCore.coregarage
</code></pre></div></div>

<blockquote>
  <p>😑 Super annoying that these new Nix commands even with <em>-L</em> continue to write the text in-place for their log output.</p>
</blockquote>

<h2 id="-goal-2-compute-outpaths-for-every-package">🎯 Goal 2: Compute outPaths for every package</h2>

<p>Since we are using <em>nix search</em> the names of the packages are now following the Flakes naming convention with prefixing them with <em>legacyPackages</em>.</p>

<p>The plan here to continue with the new Nix commands and now evaluate the <em>outPath</em> of each package.</p>

<p>⚠️ This does not build the derivation.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>❯ <span class="nb">cat </span>package-names.txt | <span class="se">\</span>
xargs <span class="nt">-I</span><span class="s1">'{}'</span> sh <span class="nt">-c</span> <span class="se">\</span>
<span class="s1">'nix eval --raw ".#{}.outPath" &gt;&gt; outpaths.txt; echo &gt;&gt; outpaths.txt'</span>

❯ <span class="nb">head</span> <span class="nt">-n</span> 10 outpaths.txt
/nix/store/jrvzirqlzpylxxij8q10hramdsgk6nvx-AMB-plugins-0.8.1
/nix/store/szh7aikwz12vj1sbkf4r6vdvy1k8apym-archisteamfarm-5.1.5.3
/nix/store/mxi5pignri1z8n3lizkcp8y8m8cgfn55-AusweisApp2-1.22.2
/nix/store/6d69d3lbk6dbgqnvzccx9lpi7hj3f6i9-CHOWTapeModel-2.10.0
/nix/store/lhk99h3adbfhzdk1spcx7awky7bzhwab-ChowKick-1.1.1
/nix/store/6p5g08rrq4cf5yrs970h4qrv7drj812l-CoinMP-1.8.4
/nix/store/qlxs8yrmssi1x54a5m3q0941ha4qvaa8-coreaction-4.2.0
/nix/store/64r6kkd6cxm1jhlhl9zma80jgvidrfyw-corearchiver-4.2.0
/nix/store/mh79zi4l343fj1xa54p1a4qif38f4435-corefm-4.2.0
/nix/store/6disqp58dkyi8i3vpb6ja5sr8g4xpqxg-coregarage-4.2.0
</code></pre></div></div>

<p>⚠️ I could not seem to make this <em>parallel</em> at all since the evaluation requires a global lock.</p>

<p>Now I just need to rename <em>outpaths.txt</em> to something that indicates the Git commit I used when generating this and I am starting to
build some nice structure data 📊.</p>

<h2 id="bonus-work-and-collaboration">Bonus Work and Collaboration</h2>

<p>Of course what I am doing here is only generating for my current <em>builtins.currentSystem</em> (i.e. x86_64) and will not generate for all other supported platforms.</p>

<p>There are also recursive subtrees within <em>nixpkgs</em> such as <em>pkgsStatic</em> or <em>pkgsMusl</em> that I don’t believe are returned from <em>nix search</em> and therefore I am not
detecting those output paths.</p>

<p>I would like to continue to understand better how tools such as Hydra generate all attributes for all systems.</p>

<p>If this problem and goal to build an <em>outputPath</em> reverse index for Nix sounds interesting, reach out to <a href="mailto:farid.m.zakaria@gmail.com">me</a>!
I would love to collaborate.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Nix is an amazing tool, unfortunately doing simple things can be quite challenging. This is a little write-up of my attempt to try and accomplish what I would have thought to be a simple thing; computing all store paths for every attribute in nixpkgs. Why would I want to do such a thing? I had some /nix/store entries on my system and I wanted to revisit the exact nixpkgs commit with which it was built to debug something. Without this reverse index you are pretty much out of luck for figuring it out. I want to give early shoutout to other similar tools in this space that let you do meta searches over nixpkgs such as Nix Package Versions and Pkgs on Nix.]]></summary></entry><entry><title type="html">Using an overlay filesystem to improve Nix CI builds</title><link href="https://fzakaria.com/2021/09/10/using-an-overlay-filesystem-to-improve-nix-ci-builds.html" rel="alternate" type="text/html" title="Using an overlay filesystem to improve Nix CI builds" /><published>2021-09-10T15:58:00+00:00</published><updated>2021-09-10T15:58:00+00:00</updated><id>https://fzakaria.com/2021/09/10/using-an-overlay-filesystem-to-improve-nix-ci-builds</id><content type="html" xml:base="https://fzakaria.com/2021/09/10/using-an-overlay-filesystem-to-improve-nix-ci-builds.html"><![CDATA[<p>Using Nix in our CI system has been a huge <em>boon</em>. Through Nix we have a level of guarantee of reproducibility between our local development environment and our CI platform. 🙌</p>

<p>Our CI infrastructure leverages containers (don’t they all now?) for each job and we explored different solutions to reduce the cost of constantly downloading the <em>/nix/store</em> necessary for the build.</p>

<!--more-->

<h2 id="fat-docker-image">Fat Docker image</h2>

<p>At first, we implemented a solution where the <em>/nix/store</em> was embedded within the Docker image itself.</p>

<p>This was an interesting choice for a few reasons:</p>
<ol>
  <li>We could disable building from source or any substituters in the build and guarantee the image itself had the complete necessary dependency closure.</li>
  <li>The images themselves are cached locally on the underlying host, meaning subsequent CI jobs don’t pay the cost of downloading <em>/nix/store</em> contents</li>
  <li>Changes to our dependency closure were annoying since it required rebuilding a new Docker image</li>
  <li>The docker image was very large and minor changes resulted in a complete new large layer – it wasn’t incremental at all.</li>
</ol>

<h2 id="overlay-filesystem">Overlay Filesystem</h2>

<p>We decided to seek out alternatives where we can remove the prebuild <em>/nix/store</em> from the container but still reduce the cold-boot cost for CI jobs.</p>

<p>The <strong>caveat</strong> to the solution we seeked is that the Docker image still <strong>installed</strong> Nix which meant it created a <em>/nix/store</em> entry and the necessary <em>~/.nix_profile</em> symlinks.</p>

<blockquote>
  <p><a href="https://github.com/torvalds/linux/commit/e9be9d5e76e34872f0c37d72e25bc27fe9e2c54c">Overlayfs</a> allows one, usually read-write, directory tree to be
overlaid onto another, read-only directory tree.  All modifications
go to the upper, writable layer.</p>
</blockquote>

<p>Let’s explore how we might go about setting this up! Let’s use a <em>dummy example</em>.</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> ubuntu</span>

<span class="c"># Let's make a dummy nix-store</span>
<span class="k">RUN </span><span class="nb">mkdir</span> <span class="nt">-p</span> /nix/store

<span class="c"># let's put a dummy derivation</span>
<span class="k">RUN </span><span class="nb">echo</span> <span class="s2">"hello"</span> <span class="o">&gt;</span> /nix/store/hello

<span class="c"># a dummy command</span>
<span class="k">CMD</span><span class="s"> ["/bin/bash"]</span>
</code></pre></div></div>

<p>I will also create some <em>dummy directories</em> on my <strong>host</strong>.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> /tmp/fake-nix/<span class="o">{</span>upper,workdir<span class="o">}</span>

<span class="nb">echo</span> <span class="s2">"ping"</span> <span class="o">&gt;</span> /tmp/fake-nix/upper/pong
</code></pre></div></div>

<p>Let’s spin up a docker container.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># lets run a docker and bind-mount of host /nix/store</span>
<span class="c"># in this case we called it /tmp/fake-nix</span>
<span class="c"># In reality this will be your host's /nix/store</span>
docker run <span class="nt">--privileged</span> <span class="nt">-v</span> /tmp/fake-nix:/nix/store-host <span class="se">\</span>
           <span class="nt">-it</span> <span class="si">$(</span>docker build <span class="nb">.</span> <span class="nt">--quiet</span><span class="si">)</span> /bin/bash
</code></pre></div></div>

<p>Let’s check the contents of the <em>/nix/store</em> originally.
We see that it only has out <em>hello</em> file.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>root@c32024e56f25:/# <span class="nb">ls</span> /nix/store
hello
</code></pre></div></div>

<p>Now let’s mount our overlay filesystem.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>root@c32024e56f25:/# mount <span class="nt">-t</span> overlay overlay <span class="nt">-o</span> <span class="se">\ </span>
        <span class="nv">lowerdir</span><span class="o">=</span>/nix/store,upperdir<span class="o">=</span>/nix/store-host/upper,workdir<span class="o">=</span>/nix/store-host/workdir <span class="se">\</span>
        /nix/store
</code></pre></div></div>

<p>Let’s check the contents of our <em>/nix/store</em> now.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>root@c32024e56f25:/# <span class="nb">ls</span> <span class="nt">-l</span> /nix/store
total 8
<span class="nt">-rw-r--r--</span> 1 root   root  6 Sep  9 17:15 hello
<span class="nt">-rw-r--r--</span> 1 780412 89939 5 Sep 10 16:22 pong
</code></pre></div></div>

<p>Great! Our <em>/nix/store</em> now has the contents of the host overlaid ontop of the one within the container.</p>

<p>What if we write a new file?</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>root@c32024e56f25:/# <span class="nb">echo</span> <span class="s2">"test"</span> <span class="o">&gt;</span> /nix/store/test

root@c32024e56f25:/# <span class="nb">ls</span> /nix/store
hello  pong  <span class="nb">test

</span>root@c32024e56f25:/# <span class="nb">ls</span> /nix/store-host/upper/
pong  <span class="nb">test</span>
</code></pre></div></div>

<p>We see that it created the file in the <em>upper</em> directory.</p>

<p>What if we update a file in the <em>lower</em> directory?</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>root@c32024e56f25:/# <span class="nb">echo</span> <span class="s2">"world"</span> <span class="o">&gt;</span> /nix/store/hello

root@c32024e56f25:/# <span class="nb">ls</span> /nix/store-host/upper/
hello  pong  <span class="nb">test</span>
</code></pre></div></div>

<p>Ok cool – it moved it to our <em>upper</em> directory.</p>

<p>What if we delete the file?</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>root@c32024e56f25:/# <span class="nb">rm</span> /nix/store/hello

root@c32024e56f25:/# <span class="nb">ls</span> /nix/store            
pong  <span class="nb">test

</span>root@c32024e56f25:/# <span class="nb">ls</span> <span class="nt">-l</span> /nix/store-host/upper      
total 8
c--------- 2 root   root  0, 0 Sep 11 02:22 hello
<span class="nt">-rw-r--r--</span> 1 780412 89939    5 Sep 10 16:22 pong
<span class="nt">-rw-r--r--</span> 1 root   root     5 Sep 11 02:19 <span class="nb">test</span>
</code></pre></div></div>

<p>The <em>hello</em> file still exists in the <em>upper</em> directory but it’s a tombstone file now.
This is to distinguish it from the fact it’s been deleted while still existing in the <em>lower</em> directory.</p>

<p>Awesome – what’s great too is that the overlay filesystem that’s supported natively in Linux gives you near native performance.</p>

<blockquote>
  <p>The implementation differs from other “union filesystem”
implementations in that after a file is opened all operations go
directly to the underlying, lower or upper, filesystems.  This
simplifies the implementation and allows native performance in these
cases.</p>
</blockquote>

<p>Hope that helps others in trying to speedup their builds. 🤓</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Using Nix in our CI system has been a huge boon. Through Nix we have a level of guarantee of reproducibility between our local development environment and our CI platform. 🙌 Our CI infrastructure leverages containers (don’t they all now?) for each job and we explored different solutions to reduce the cost of constantly downloading the /nix/store necessary for the build.]]></summary></entry><entry><title type="html">The yak shave for reproducibility</title><link href="https://fzakaria.com/2021/09/03/the-yak-shave-for-reproducibility.html" rel="alternate" type="text/html" title="The yak shave for reproducibility" /><published>2021-09-03T01:01:00+00:00</published><updated>2021-09-03T01:01:00+00:00</updated><id>https://fzakaria.com/2021/09/03/the-yak-shave-for-reproducibility</id><content type="html" xml:base="https://fzakaria.com/2021/09/03/the-yak-shave-for-reproducibility.html"><![CDATA[<p>I have been on a mission to bring reproducibility through the use of <a href="http://nixos.org/">Nix</a> into my workplace as we envision the next version of our development environment.</p>

<p>Similar to the movies I watch that take place in space, it only takes a small hole to destroy your hermetic environment. 🧑‍🚀</p>

<!--more-->

<p>I’ve <a href="/2020/11/12/debugging-a-jnr-ffi-bug-in-nix.html">written previously</a> about my encounters trying to remove all impurities within a JVM environment.</p>

<p>I’ve actually <a href="https://github.com/NixOS/nixpkgs/pull/123708">upstreamed</a> fixing the default <em>java.library.path</em> for the OpenJDK distributed by Nixpkgs. 🙌</p>

<p>Awesome! That should have solved my problem, right ?…</p>

<p>Unfortunately, <em>impurities</em> are tough to stamp out. NixOS is trying to accomplish a paradigm shift by dismissing the <em><a href="https://en.wikipedia.org/wiki/Filesystem_Hierarchy_Standard">filesystem hierarchy standard</a></em> but it is deeply rooted in assumptions when people build for Linux.</p>

<p>In my specific case, the search paths for the JNR libraries were <a href="https://github.com/jnr/jnr-ffi/blob/2161d3d875869bdaf292b0e9267bb0fbdb0f3f2b/src/main/java/jnr/ffi/LibraryLoader.java#L511">hardcoded</a>.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// paths should have no duplicate entries and have insertion order</span>
<span class="nc">LinkedHashSet</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">paths</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">LinkedHashSet</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;();</span>
<span class="k">try</span> <span class="o">{</span>
    <span class="n">paths</span><span class="o">.</span><span class="na">addAll</span><span class="o">(</span><span class="n">getPropertyPaths</span><span class="o">(</span><span class="s">"jnr.ffi.library.path"</span><span class="o">));</span>
    <span class="n">paths</span><span class="o">.</span><span class="na">addAll</span><span class="o">(</span><span class="n">getPropertyPaths</span><span class="o">(</span><span class="s">"jaffl.library.path"</span><span class="o">));</span>
    <span class="c1">// Add JNA paths for compatibility</span>
    <span class="n">paths</span><span class="o">.</span><span class="na">addAll</span><span class="o">(</span><span class="n">getPropertyPaths</span><span class="o">(</span><span class="s">"jna.library.path"</span><span class="o">));</span>
    <span class="c1">// java.library.path should take care of Windows defaults</span>
    <span class="n">paths</span><span class="o">.</span><span class="na">addAll</span><span class="o">(</span><span class="n">getPropertyPaths</span><span class="o">(</span><span class="s">"java.library.path"</span><span class="o">));</span>
<span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">Exception</span> <span class="n">ignored</span><span class="o">)</span> <span class="o">{</span>
<span class="o">}</span>
<span class="k">if</span> <span class="o">(</span><span class="nc">Platform</span><span class="o">.</span><span class="na">getNativePlatform</span><span class="o">().</span><span class="na">isUnix</span><span class="o">())</span> <span class="o">{</span>
    <span class="c1">// order is intentional!</span>
    <span class="n">paths</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"/usr/local/lib"</span><span class="o">);</span>
    <span class="n">paths</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"/usr/lib"</span><span class="o">);</span>
    <span class="n">paths</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"/lib"</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<blockquote>
  <p>I’ve sent <a href="https://github.com/jnr/jnr-ffi/pull/264/">a fix upstream</a> please feel free to comment.</p>
</blockquote>

<p>This meant that I had to make sure the <em>java.library.path</em> resolved to the <em>glibc</em> I am using in Nixpkgs first.</p>

<p>I couldn’t get rid of the <code class="language-plaintext highlighter-rouge">JAVA_TOOL_OPTIONS</code> just yet. 😤</p>

<h2 id="whats-the-cost-of-all-this">What’s the cost of all this?</h2>

<p>First off, the JDK emits <code class="language-plaintext highlighter-rouge">JAVA_TOOL_OPTIONS</code> via <em>stderr</em> which is <a href="https://github.com/AdoptOpenJDK/openjdk-jdk8u/blob/9a751dc19fae78ce58fb0eb176522070c992fb6f/hotspot/src/share/vm/runtime/arguments.cpp#L3753">non-configurable</a>.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="p">(</span><span class="n">os</span><span class="o">::</span><span class="n">getenv</span><span class="p">(</span><span class="n">name</span><span class="p">,</span> <span class="n">buffer</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">buffer</span><span class="p">))</span> <span class="o">&amp;&amp;</span>
    <span class="o">!</span><span class="n">os</span><span class="o">::</span><span class="n">have_special_privileges</span><span class="p">())</span> <span class="p">{</span>
  <span class="n">JavaVMOption</span> <span class="n">options</span><span class="p">[</span><span class="n">N_MAX_OPTIONS</span><span class="p">];</span>      <span class="c1">// Construct option array</span>
  <span class="n">jio_fprintf</span><span class="p">(</span><span class="n">defaultStream</span><span class="o">::</span><span class="n">error_stream</span><span class="p">(),</span>
            <span class="s">"Picked up %s: %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">name</span><span class="p">,</span> <span class="n">buffer</span><span class="p">);</span>
</code></pre></div></div>

<p>This is frustrating because plenty of tools (i.e. IntelliJ) assume failure if anything is emitted to <em>stderr</em>.</p>

<p>Secondly, we have many developers that are being hit by this particular workflow:</p>

<ol>
  <li>Developer sets up their IntelliJ JRuby SDK to point to Jruby which points to <em>/nix/store</em> path <strong>A</strong> linked with glibc <strong>B</strong>.</li>
  <li>Developer changes their Git branch to a different point in time where JRuby points to a different <em>/nix/store</em> path <strong>X</strong> linked with glibc <strong>Z</strong>.</li>
  <li>The developer restarts IntelliJ and picks up the new environment variable <em>JAVA_TOOL_OPTIONS</em> 💥</li>
</ol>

<blockquote>
  <p>If you read <a href="/2020/11/12/debugging-a-jnr-ffi-bug-in-nix.html">my earlier post</a>, you’ll understand why <em>JAVA_TOOL_OPTIONS</em> includes glibc.</p>
</blockquote>

<p>That <em>JAVA_TOOL_OPTIONS</em> references glibc <strong>Z</strong> but their JRuby SDK is pointing to JRuby <strong>A</strong> which was built against glibc <strong>B</strong>.</p>

<p>They are then graced with this <em>wonderful</em> message.</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>java.lang.UnsatisfiedLinkError: /nix/store/cvr0kjg2q7z2wwhjblx6c73rv422k8cm-glibc-2.33-47/lib/libc.so.6: undefined symbol: _dl_catch_error_ptr, version GLIBC_PRIVATE
	at jnr.ffi.provider.jffi.NativeLibrary.loadNativeLibraries(NativeLibrary.java:93)
</code></pre></div></div>

<p>Ugh! The fix here is straightforward ultimately – the developer needs to be mindful of their JRuby SDK set in IntelliJ and keep it in sync with their local checkout.</p>

<p>Unfortunately it’s a sharp edge many are running into; and I don’t blame them!</p>

<p>We will be thinking of some sane ways to keep these two tools in sync so that we can remain on upstream for now… 🤔</p>]]></content><author><name></name></author><summary type="html"><![CDATA[I have been on a mission to bring reproducibility through the use of Nix into my workplace as we envision the next version of our development environment. Similar to the movies I watch that take place in space, it only takes a small hole to destroy your hermetic environment. 🧑‍🚀]]></summary></entry></feed>